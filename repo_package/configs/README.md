# Configs

`config.yaml` (final model hyperparameters and split boundaries) is included here.

`environment_snapshot.json` and `dataverse_raw_metadata.json` were generated live in the
Colab session (hardware detection and Dataverse API responses respectively) and were not
transferred out of that session as standalone files. Both are trivially regenerable by
running the corresponding cells in `notebooks/01_data_inspection.ipynb`, Sections 1 and 2.
